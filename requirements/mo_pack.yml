name: mo-pack-dev

channels:
  - conda-forge
  - nodefaults

dependencies:

# setup dependencies
  - setuptools >=79
  - setuptools-scm >=8

# core dependencies
  - numpy >1.21
  - cython >=3
  - libmo_unpack

# developer dependencies
  - pip
  - pre-commit

# test dependencies
  - pytest
